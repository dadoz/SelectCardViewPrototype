<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/material_indigo600</item>
        <item name="colorPrimary">@color/material_indigo400</item>
        <item name="actionBarTheme">@style/ThemeOverlay.AppTheme.ActionBar</item>
    </style>

    <style name="ThemeOverlay.AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <item name="android:textColorPrimary">@color/material_grey200</item>
        <!-- subtitle text color -->
        <!--<item name="android:textColorSecondary">?android:textColorPrimary</item>-->
        <!-- action menu item text color -->
        <!--<item name="actionMenuTextColor">?android:textColorPrimary</item>-->
        <!-- action menu item icon color - only applies to appcompat-v7 icons :( -->
        <!--<item name="colorControlNormal">?android:textColorPrimary</item>-->
    </style>
</resources>
